// math.ixa — a dependency module. Files provide a module named by
// their stem; `use math;` in main.ixa resolves against it.

data Vec2 { x: i32; y: i32; }

fn double(x: i32) -> i32 {
    return x * 2;
}

fn norm(v: Vec2) -> i32 {
    return v.x * v.x + v.y * v.y;
}
